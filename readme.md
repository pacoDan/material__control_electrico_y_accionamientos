Opción 2: Habilitar nombres largos en Windows
```sh
git config core.longpaths true
```
para ver mas detalles de cualquier comando git:
```sh
# Modo verbose
git add -v .

# Ver el estado detallado
git status --porcelain

# Ver configuración actual
git config --list
```